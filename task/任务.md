# 任务1.0
    1. 在文件操作时，默认为当前项目的路径
    2. tool 中的文件操作时，默认为当前选中项目的路径

# 任务1.1
    1. 工具调用渲染默认收缩状态
    2. 会话添加清空会话内容功能
    3. token 统计功能缺失
    4. 会话列表中，会话名称过长时，显示省略号

# 任务1.2
    1. 添加终止会话功能
    2. 添加会话压缩功能
    3. 会话压缩后，会话名称前添加压缩图标
    4. tool 的请求参数和返回参数存入数据库中，在查看会话详情时，展示请求参数和返回参数