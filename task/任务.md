# 任务1.0
    1. 在文件操作时，默认为当前项目的路径
    2. tool 中的文件操作时，默认为当前选中项目的路径

# 任务1.1
    1. 工具调用渲染默认收缩状态
    2. 会话添加清空会话内容功能
    3. token 统计功能缺失
    4. 会话列表中，会话名称过长时，显示省略号

# 任务1.2
    1. 添加终止会话功能
    2. 添加会话压缩功能
    3. 会话压缩后，会话名称前添加压缩图标
    4. tool 的请求参数和返回参数存入数据库中，在查看会话详情时，展示请求参数和返回参数

# 任务1.3
    1. 菜单中接口文档转移到最下面显示
    2. 在项目管理中添加索引项目功能 对项目进行倒排索引，使用 goleveldb 存储
    3. 进行手动索引项目，索引项目后，在会话列表中可以根据项目名称搜索会话
    4. 提供对应的索引 tool

# 任务1.4
    1. 添加选择某一条重新发送的功能
    2. 处理模型的思考模式 <think>
    3. 前端使用 <Thinking v-model="senderValue" content="欢迎使用 Element-Plus-X" status="start" />  通过 status 属性设置组件的状态，一共有四个默认状态，分别是 start、thinking、end、error

# 任务1.5
    1. 添加 MCP 管理模块
    2. 实现 MCP 通用客户端，支持添加、删除、修改 MCP    

# 任务 1.6
    1. 实现 SKILL 管理模块
    2. 实现 SKILL 通用客户端，支持添加、删除、修改 SKILL

# 任务 1.7
    1. 实现 SUBAGENT 功能

# TODO
添加更多操作文件类的 tool, 比如 diff_file